import {PrismaClient,Difficulty} from '@prisma/client';const prisma=new PrismaClient();
const careerNames=['Frontend Developer','Backend Developer','Full-Stack Developer','QA Automation Engineer','UI/UX Developer'];const nodeNames=['HTML and CSS','JavaScript Fundamentals','Git and GitHub','TypeScript','React','Next.js','API Development','Authentication','Databases','Testing','Deployment','Advanced Project'];const missionNames=['Build a responsive landing page','Fix a login redirect loop','Create a REST API','Build authentication with protected routes','Add form validation','Optimize React rendering','Write automated browser tests','Deploy a Next.js app','Design a reusable component system','Build a full-stack dashboard','Create a component library','Add database pagination','Secure an API route','Build a webhook consumer','Test an authentication flow','Improve Core Web Vitals','Create an accessible modal','Build optimistic updates','Ship a real-time chat','Build a complete portfolio'];const achievementNames=['First Mission','First Deployment','Authentication Master','API Builder','Frontend Explorer','Backend Explorer','Seven-Day Streak','Five Missions Completed','Ten Missions Completed','Full-Stack Challenger','Bug Hunter','Testing Specialist'];
async function main(){for(const name of careerNames){const slug=name.toLowerCase().replaceAll(/[^a-z0-9]+/g,'-');const career=await prisma.career.upsert({where:{slug},update:{},create:{name,slug,description:`A structured ${name} campaign.`}});for(const [position,title] of nodeNames.entries())await prisma.roadmapNode.upsert({where:{careerId_slug:{careerId:career.id,slug:title.toLowerCase().replaceAll(/[^a-z0-9]+/g,'-')}},update:{},create:{careerId:career.id,title,slug:title.toLowerCase().replaceAll(/[^a-z0-9]+/g,'-'),description:`Master ${title} through practical work.`,position,xpReward:position===11?1000:250}});if(name==='Full-Stack Developer')for(const [i,title] of missionNames.entries()){const slug=title.toLowerCase().replaceAll(/[^a-z0-9]+/g,'-');const difficulty=i>17?Difficulty.BOSS:i>11?Difficulty.ADVANCED:i>5?Difficulty.INTERMEDIATE:Difficulty.BEGINNER;await prisma.mission.upsert({where:{slug},update:{},create:{careerId:career.id,slug,title,description:`Complete a production-ready ${title.toLowerCase()} challenge.`,scenario:'A product team needs a secure, accessible, documented solution.',difficulty,xpReward:difficulty==='BOSS'?1000:difficulty==='ADVANCED'?500:difficulty==='INTERMEDIATE'?250:100,estimatedMinutes:120+i*30,boss:difficulty==='BOSS'}})}}for(const name of achievementNames){const slug=name.toLowerCase().replaceAll(/[^a-z0-9]+/g,'-');await prisma.achievement.upsert({where:{slug},update:{},create:{slug,name,description:`Unlocked by completing the ${name} milestone.`,icon:'award',xpReward:100}})}}main().finally(()=>prisma.$disconnect());
